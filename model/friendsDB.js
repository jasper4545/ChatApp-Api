const { DataTypes } = require("sequelize")
const sequlize = require("sequelize")
const fsPromise = require("fs").promises
const fs = require("fs")
const path = require("path")

async function genSchema(friend){
  
}
